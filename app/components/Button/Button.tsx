import { Button, ButtonProps } from "@mantine/core";

/**
 * Primary UI component for user interaction
 */
export const StoryButton = (props: ButtonProps) => {
  return <Button {...props}>Hello</Button>;
};
